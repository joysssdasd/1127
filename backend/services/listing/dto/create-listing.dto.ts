import { CreateListingInput } from '../../../shared/contracts/listing';

/**
 * Decorator-free DTO placeholder to keep compatibility with validation logic.
 * For now it simply mirrors {@link CreateListingInput}.
 */
export type CreateListingDto = CreateListingInput;
